# Public build-time vars (embedded in client JS by Vite during `astro build`).
# Not secrets — visible in the browser bundle. Committed so CF Workers Builds
# bakes them in CI. Turnstile site key must allow rubrion.ai + rubrion.com.br.
VITE_SUPPORT_WORKER_URL=https://support-email-worker.rubrion.workers.dev/submit
VITE_TURNSTILE_SITE_KEY=0x4AAAAAADEAz2L_EVpiTBsg
